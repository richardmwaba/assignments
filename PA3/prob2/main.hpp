#ifndef MAIN_HPP
#define MAIN_HPP

#include <iostream>


// Produce item
void * produce(void * arg);

// Consume itam
void * consume(void * arg);

// Generate random string
char generate_random_alphabet(int n);

#endif